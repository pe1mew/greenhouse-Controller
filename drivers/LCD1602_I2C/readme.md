# LCD1602 I2C

Driver for the Waveshare LCD1602 I2C module (HD44780 LCD controlled by a
PCF8574A I/O expander at address 0x3E).

Used by T8 (UI / Display) to render two-line 16-character status screens.
Depends on LIB-2 (`i2c/`).
